{
  "name": "tinkerai",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-component-svgs.mjs && node scripts/build-component-visuals.mjs && vite",
    "build:visuals": "node scripts/build-component-visuals.mjs",
    "generate:svgs": "node scripts/generate-component-svgs.mjs",
    "build": "node scripts/generate-component-svgs.mjs && node scripts/build-component-visuals.mjs && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  }
}
